#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n, x, fir, sec;
    
    vector<int> arr;
    for(int i = 0; i < n; i++) {
        cin >> x;
        arr.push_back(x);

    }

    for( int j = 1; j <= n; j++) {
        if ( j != arr[j-1]) {
            fir = j;
            for(;;;)
        }
    }


}